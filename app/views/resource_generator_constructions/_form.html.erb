<%= form_with(model: resource_generator_construction) do |form| %>
  <% if resource_generator_construction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(resource_generator_construction.errors.count, "error") %> prohibited this resource_generator_construction from being saved:</h2>

      <ul>
        <% resource_generator_construction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div >
    <%= form.hidden_field :resource_generator_type,value:"Mine" %>
  </div>

  <div>
    <%= form.submit "Mine", class:"bg-teal-200 border-4 border-black rounded-lg p-3 text-xl"%>
  </div>
<% end %>
<%= form_with(model: resource_generator_construction) do |form| %>
  <% if resource_generator_construction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(resource_generator_construction.errors.count, "error") %> prohibited this resource_generator_construction from being saved:</h2>

      <ul>
        <% resource_generator_construction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div >
    <%= form.hidden_field :resource_generator_type,value:"Castle" %>
  </div>

  <div>
    <%= form.submit "Castle", class:"bg-teal-200 border-4 border-black rounded-lg p-3 text-xl"%>
  </div>
<% end %>
<%= form_with(model: resource_generator_construction) do |form| %>
  <% if resource_generator_construction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(resource_generator_construction.errors.count, "error") %> prohibited this resource_generator_construction from being saved:</h2>

      <ul>
        <% resource_generator_construction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div >
    <%= form.hidden_field :resource_generator_type,value:"Embassy" %>
  </div>

  <div>
    <%= form.submit "Embassy", class:"bg-teal-200 border-4 border-black rounded-lg p-3 text-xl"%>
  </div>
<% end %>
