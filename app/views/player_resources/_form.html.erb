<%= form_with(model: player_resource) do |form| %>
  <% if player_resource.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(player_resource.errors.count, "error") %> prohibited this player_resource from being saved:</h2>

      <ul>
        <% player_resource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :player_id, style: "display: block" %>
    <%= form.text_field :player_id %>
  </div>

  <div>
    <%= form.label :resource_quantity, style: "display: block" %>
    <%= form.number_field :resource_quantity %>
  </div>

  <div>
    <%= form.label :resource_type, style: "display: block" %>
    <%= form.text_field :resource_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
